{
  "name": "Basic Python",
  "build": { "dockerfile": "Dockerfile" },
  "remoteUser": "vscode",
  "customizations": {
    "vscode": {
      "extensions": ["ms-python.python"]
    }
  },
  "tasks": {
    "test": "pytest",
    "build": "pip install -r requirements.txt",
    "launch": "flask run"
  }
}
